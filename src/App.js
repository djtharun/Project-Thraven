import './App.css';
import Home from './pages/Home';

function App() {
  return (
    <Home />
  );
}

export default App;
// This code is a simple React component that renders a full-screen message indicating that "Project Thraven is ALIVE".
// It uses Tailwind CSS for styling, applying a dark background color, white text, and centering the content both vertically and horizontally.
// The component is named App and is exported as the default export of the module.
// The component is styled using Tailwind CSS classes:
// - `h-screen`: Sets the height of the div to 100% of the viewport height.
// - `bg-gray-900`: Sets the background color to a dark gray.
// - `text-white`: Sets the text color to white.
// - `flex`: Applies Flexbox layout to the div.
// - `items-center`: Centers the items vertically within the flex container.
// - `justify-center`: Centers the items horizontally within the flex container.
// - `text-4xl`: Sets the font size to 4xl (extra large).
// - `font-bold`: Sets the font weight to bold.
